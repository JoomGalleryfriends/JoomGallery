name: CI JoomGallery

on:
  pull_request:
    branches:
      - master
      - 'v*'

jobs:
  prepare:
    name: PR preparations
    if: github.base_ref == 'master' || startsWith(github.base_ref, 'v')
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: composer

      - name: Install component dependencies
        run: composer install --working-dir=src/administrator/com_joomgallery --no-dev --prefer-dist --no-ansi --no-interaction --no-progress --optimize-autoloader

      - name: Clean up unnecessary files
        run: |
          rm -rf src/administrator/com_joomgallery/composer.json src/administrator/com_joomgallery/composer.lock

      - name: Cleanup pel directory
        run: |
          PEL_DIR="src/administrator/com_joomgallery/vendor/fileeye/pel"
          find "$PEL_DIR" -mindepth 1 -maxdepth 1 ! -name 'src' ! -name 'autoload.php' ! -name 'composer.json' -exec rm -rf {} +

      - name: Sanitize PR title for filename
        id: pr
        run: |
          SAFE_TITLE=$(echo "${{ github.event.pull_request.title }}" | tr -cd '[:alnum:]._-')
          echo "SAFE_TITLE=${SAFE_TITLE}" >> $GITHUB_OUTPUT

      - name: Upload artifact (GitHub will zip it)
        uses: actions/upload-artifact@v6
        with:
          name: JoomGallery-${{ steps.pr.outputs.SAFE_TITLE }}
          path: |
            joomgallery.xml
            script.php
            src
          retention-days: 60

  code-style-php:
    name: Check PHP code style
    runs-on: ubuntu-latest
    needs: [prepare]

    steps:
      - uses: actions/checkout@v5
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          tools: composer
          coverage: none

      # Cache composer downloads + vendor to speed up installs
      - name: Cache Composer
        uses: actions/cache@v5
        with:
          path: |
            ~/.composer/cache/files
            vendor
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      # Install WITH dev so cs tools are available
      - name: Install dependencies (with dev)
        run: composer install --prefer-dist --no-ansi --no-interaction --no-progress

      - name: Run PHP-CS-Fixer
        env:
          PHP_CS_FIXER_IGNORE_ENV: true
        run: ./vendor/bin/php-cs-fixer fix -vvv --dry-run --diff

      - name: Run PHPCS
        run: ./vendor/bin/phpcs --extensions=php -p --standard=ruleset.xml --runtime-set ignore_warnings_on_exit 1 src

      # - name: Run PHPStan
        if: ${{ hashFiles('joomla/includes/framework.php') != '' }}
        run: ./vendor/bin/phpstan analyse

      # Rector currently proposes changes across the existing codebase.
      # Re-enable this check after the initial Rector migration is complete.
      # - name: Run Rector dry-run
      #   run: ./vendor/bin/rector process --dry-run
